# Discordia Hyprland Autostart Configuration
# Tray applications - launch minimized to system tray

exec-once = protonvpn-app --start-minimized
exec-once = trayscale --hide-window
exec-once = discord --start-minimized
