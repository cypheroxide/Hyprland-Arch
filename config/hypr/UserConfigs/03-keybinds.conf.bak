###################
### KEYBINDINGS ###
###################

# See https://wiki.hypr.land/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hypr.land/Configuring/Binds/ for more
bind = $mainMod, T, exec, $terminal
bind = $mainMod, Q, killactive,
bind = $mainMod, M, exit,
bind = $mainMod, O, exec, com.obsproject.Studio
bind = SuperShift, E, exec, $fileManager
bind = SuperShift, T, exec, thunderbird-beta
bind = $mainMod, E, exec, kitty spf
bind = $mainMod, F, togglefloating,
bind = $mainMod, Space, exec, $menu
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, togglesplit, # dwindle
bind = SuperShift, B, exec, qutebrowser
bind = $mainMod, R, exec, rofi -show drun
bind = $mainMod, slash, exec, ~/.config/hypr/Scripts/keybind-help.sh
bind = $mainMod, S, exec, kitty --class=fzf-popup -e ~/.config/hypr/Scripts/fzf-search.sh
bind = SuperCtrl, R, exec, rofi
bind = $mainMod, B, exec, ~/.local/bin/launch-brave.sh
bind = $mainMod, W, exec, warp-terminal
bind = $mainMod, TAB, overview:toggle
bind = $mainMod, A, hyprexpo:expo, toggle
bind = SuperControl, V, exec, vscodium --no-sandbox

# Important ecosystem tools
bind = $mainMod, L, exec, hyprlock 
bind = SUPER, V, exec, cliphist list | rofi -dmenu -display-columns 2 | cliphist decode | wl-copy

# Rotate wallpaper
bind = SuperShift, L, exec, ~/.config/hypr/Scripts/hyprpaper-rotate.sh

# Screenshot a window
bind = $mainMod, PRINT, exec, hyprshot -m window

# Screenshot a monitor
bind = , PRINT, exec, hyprshot -m output

# Screenshot a region
bind = SuperShift, S, exec, hyprshot -m region
bind = SuperShift, escape, exec, kitty -e btop


# Dropdown terminal with tdrop
bind = $mainMod, F12, exec, ~/.local/bin/dropdown-terminal

# Graphics mode toggle (hybrid <-> nvidia)
bind = $mainMod ALT, G, exec, ~/.config/hypr/Scripts/toggle-graphics.sh


# Update Wofi Categories + Applications
bind = $mainMod ALT, Space, exec, ~/.local/bin/regenerate-app-menu --force


# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, ., togglespecialworkspace, magic
bind = $mainMod SHIFT, ., movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

# Qalculate: launch on Calculator key (Fn+F12)
bind = , XF86Calculator, exec, qalculate-gtk
