# FZF Search Configuration
# Add directories to exclude from searches (one per line)
# Use relative paths from $HOME or absolute paths
# Examples:
#   .local/share/Steam
#   .cargo
#   VirtualBox VMs

# Default exclusions (git, cache, node_modules are always excluded)
.git
.cache
node_modules

# Add your custom exclusions below:
.local/share/Steam
.steam
.wine
.cargo/registry
VirtualBox VMs
.mozilla
.config/BraveSoftware
.config/google-chrome
